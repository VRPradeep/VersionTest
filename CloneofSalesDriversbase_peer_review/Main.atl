
[[overviewNarrative]]

[[products_desc=top(products_desc,numInsightsByThreshold(map(products_desc,x->x[1])));
//products_desc=top(products_desc,numInsightsBySD(map(products_desc,x->x[1])));
country_results=map(products_desc,x->countryAnalytics(filter(data,y->y[product_idx]==x[0])));
forAll(country_results,x->productNarrative(x))]]

Adding some text to check the commit1 version.

Addign commit 2 to the version.
